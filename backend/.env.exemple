# 🔗 URL de conexão com o banco de dados PostgreSQL
# Formato: postgres://usuario:senha@host:porta/nomedobanco
# Exemplo: postgres://postgres:minhasenha@localhost:5432/meubanco
DATABASE_URL= 

# 🔐 Chave secreta para geração e verificação de tokens JWT
# Deve ser uma string aleatória e segura. Usada na autenticação dos usuários.
# Exemplo: JWT_SECRET=meuSegredoSuperSeguro123!
JWT_SECRET=

# 🔑 Google OAuth - Client ID
# Encontrado no Google Cloud Console na configuração das credenciais OAuth 2.0
GOOGLE_CLIENT_ID=

# 🔒 Google OAuth - Client Secret
# Também encontrado no Google Cloud Console junto com o Client ID
GOOGLE_CLIENT_SECRET=

# 🌐 URL de redirecionamento autorizada no Google Cloud
# É para onde o Google irá enviar o usuário após o login bem-sucedido
# Exemplo (para desenvolvimento local): http://localhost:3333/auth/google/callback
GOOGLE_REDIRECT_URI=
